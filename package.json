{
  "name": "server",
  "version": "1.0.0",
  "description": "cfp project",
  "main": "index.js",
  "scripts": {
    "build": "cd client && npm run build",
    "client-install": "cd client && npm install",
    "heroku-postbuild": "npm run client-install && npm run build",
    "start": "node index.js",
    "client": "cd client && npm start"
  },
  "author": "tekenna",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "mongoose": "^5.13.3",
    "nodemon": "^2.0.12"
  },
  "devDependencies": {}
}
